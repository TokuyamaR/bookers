<!DOCTYPE html>

<html>
	<head>
		<meta charset="utf-8">
		<link rel="stylesheet" href="edit.scss">
		<title>編集画面</title>
	</head>
	<body>
		<div class="edit-wrapper">
			<h1 class="edit-title">編集画面</h1>
			<div class="edit-container">
				<%= form_for(@post) do |f| %>
					<div class="book-title">
						<h4>書籍名</h4>
						<%= f.text_field :title %>
					</div>
					<div class="book-about">
						<h4>書評（本文）</h4>
						<%= f.text_area :body %>
					</div>
					<%= f.submit '更新' %>
				<% end %>
			</div>
			<div class="from-edit-links">
				<%= link_to "詳細", post_path(@post.id),:style=>"color:orange;" %>
				|
				<%= link_to "戻る", index_path,:style=>"color:orange;" %>
			</div>
		</div>
	</body>
</html>
